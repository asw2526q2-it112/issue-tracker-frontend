# Django REST API base URL (no trailing slash).
NEXT_PUBLIC_API_BASE_URL=https://issue-tracker-4lug.onrender.com
# Local Django dev server (uncomment + comment the line above):
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
